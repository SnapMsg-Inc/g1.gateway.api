version: '3.8'
services: 
    api-gateway:
        image: api-gateway
        build: .
        container_name: api-gateway
        ports:
            - 3000:3000
        command:
            uvicorn src.main:app --host 0.0.0.0 --port 3000
