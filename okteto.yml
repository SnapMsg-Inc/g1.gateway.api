name: g1.gateway.api

build:
  api-gateway:
    context: .
    dockerfile: Dockerfile

    
deploy:
  compose:
    file: docker-compose.yml

dev:
  api-gateway:
    forward:
      - 3000:3000
    sync:
      - .:/usr/snapmsg-gateway

